## Bundled Notebook Regression Check (Static Analysis)

### Check 1: isBundled usage in notebooknodecontroller.cpp
isBundled is used ONLY for:
- Line 330-331: Tags section (only bundled notebooks have tags)
- Line 354-355: Mark action (only bundled notebooks support marking)
- Line 450-453: Delete action (bundled vs raw recycle bin behavior)

NO isBundled guard exists around Open actions. UNCHANGED by this fix.

### Check 2: addOpenActions has no notebook-type guard
Line 132: `addOpenActions(menu, p_nodeId, isFolder);` — called unconditionally
Line 205-211: Implementation only checks `!p_nodeId.isValid()` and `!p_isFolder`
No notebook type, no isBundled, no raw-specific logic.

### Conclusion
The fix does NOT introduce any notebook-type discrimination in the Open action path.
Bundled notebooks continue to work exactly as before because:
1. nodeInfoFromNodeId() looks up the same m_nodeCache that stores all node types
2. The fallback to indexFromNodeId is still available if direct lookup fails
3. No new conditions were added to addOpenActions or createContextMenu
